#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <ctype.h>
#include <math.h>

int main (){
    printf("\t\t\t\tLetra K - Cap. 5");
    setlocale(LC_ALL, "Portuguese");
int N, F;
N = 1;
    while(N<=10){
        if(!(N % 2 == 0)){
         F = pow((N - 1), N);
         N++;
        }else{}
    }

printf("\n\n");
system("\npause\n");
return 0;
}
